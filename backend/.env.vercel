# Vercel Production Environment Variables for Backend
# Copy these to Vercel Dashboard → Settings → Environment Variables

# Server Configuration
NODE_ENV=production
PORT=4000
FRONTEND_URL=https://documents.citronsociety.in

# MongoDB Configuration (Use MongoDB Atlas)
# Get connection string from: https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/citron-society-documents

# JWT Configuration
# Generate secret with: openssl rand -base64 32
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET
JWT_EXPIRES_IN=24h

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=REPLACE_WITH_YOUR_AWS_ACCESS_KEY
AWS_SECRET_ACCESS_KEY=REPLACE_WITH_YOUR_AWS_SECRET_KEY
AWS_REGION=ap-south-1
AWS_S3_BUCKET_NAME=REPLACE_WITH_YOUR_BUCKET_NAME

# SMTP Email Configuration
# For Gmail: Use App Password from Google Account Security
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=REPLACE_WITH_YOUR_EMAIL@gmail.com
SMTP_PASS=REPLACE_WITH_GMAIL_APP_PASSWORD
SMTP_FROM_EMAIL=noreply@citronsociety.in
